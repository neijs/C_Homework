#include <stdio.h>

int main(int argc, char *argv[]) {
    int i = 1, value, sign, result = 0;
    while (i < argc) {
        value = 0;

        sign = (*str == '-') ? -1 : 1; 
        if (*str == '+' || *str == '-')
            str++;
        for (val = 0; isdigit(*str); str++) 
            val = 10 * val + (*str - '0');
    }
        i++;
    }
}

